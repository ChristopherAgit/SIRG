# Frontend environment for Vite (development)
# Set the base URL where the backend API is reachable.
# Frontend build will use this at build-time; leave empty to use relative '/api/v1'
# Example for production: VITE_API_BASE=https://api.constantinopla.onrender.com
VITE_API_BASE=
