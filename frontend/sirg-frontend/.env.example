# Example frontend env file — copy to `.env` and adjust for your environment
# Do NOT commit secrets or production keys.
VITE_API_BASE=
# Example: VITE_API_BASE=https://api.constantinopla.onrender.com
